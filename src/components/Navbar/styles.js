import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
  title: {
    fontSize: 20,
    fontWeight: 'bold'
  },
  icon: {
    position: 'absolute', 
    right: 0,
    marginRight: 15
  }
}))